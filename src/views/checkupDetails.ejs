<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Checkup Details</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">
  <%- include("./partials/navbar") %>

  <div class="container mx-auto mt-10 px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-8">
      <h1 class="text-4xl font-semibold text-blue-600">Checkup Details</h1>
      <p class="text-gray-600 mt-2">
        Patient: <%= checkup.patient.first_name %> <%= checkup.patient.last_name %>
      </p>
    </div>

    <div class="bg-white shadow-lg rounded-lg p-6 mb-6">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <h2 class="text-xl font-medium text-gray-800 mb-4">Basic Information</h2>
          <div class="space-y-2">
            <p><span class="font-semibold">Doctor:</span> 
              Dr. <%= checkup.doctor.first_name %> <%= checkup.doctor.last_name %>
            </p>
            <p><span class="font-semibold">Checkup Type:</span> 
              <%= checkup.checkupType.type_name %>
            </p>
            <p><span class="font-semibold">Date:</span> 
              <%= new Date(checkup.checkup_date).toLocaleDateString() %>
            </p>
            <p><span class="font-semibold">Status:</span> 
              <span class="capitalize"><%= checkup.status %></span>
            </p>
          </div>
        </div>
        
        <div>
          <h2 class="text-xl font-medium text-gray-800 mb-4">Notes</h2>
          <p class="text-gray-700"><%= checkup.notes || 'No notes available.' %></p>
        </div>
      </div>
    </div>

    <div class="bg-white shadow-lg rounded-lg p-6">
      <div class="flex justify-between items-center mb-6">
        <h2 class="text-xl font-medium text-gray-800">Documents</h2>
        <a href="/checkups/<%= checkup.checkup_id %>/edit" 
           class="bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-500 transition">
          Edit Checkup
        </a>
      </div>

      <% if (checkupDocuments && checkupDocuments.length > 0) { %>
        <div class="overflow-x-auto">
          <table class="min-w-full divide-y divide-gray-200">
            <thead>
              <tr class="bg-gray-50">
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">File Name</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Type</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Upload Date</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
              </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
              <% checkupDocuments.forEach(doc => { %>
                <tr>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <%= doc.file_name %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <%= doc.file_type %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <%= new Date(doc.upload_date).toLocaleDateString() %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <a href="<%= doc.file_path %>" 
                      target="_blank" class="text-blue-600 hover:text-blue-900">
                      View
                   </a>
                  </td>
                </tr>
              <% }); %>
            </tbody>
          </table>
        </div>
      <% } else { %>
        <p class="text-gray-500">No documents uploaded yet.</p>
      <% } %>
    </div>

    <div class="mt-6 text-center">
      <a href="/checkups" class="bg-gray-600 text-white px-4 py-2 rounded-md hover:bg-gray-500 transition">
        Back to Checkups
      </a>
    </div>
  </div>
</body>
</html>